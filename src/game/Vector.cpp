//
// Created bvector_y A3-hillman on 27/11/2019.
//

#include "Vector.h"
#include <math.h>

/*
void Vector::normalise()
{
  float magnitude = sqrt((vector_x * vector_x) + (vector_y * vector_y));
  // float unit_vector[2] = { (vector_x / magnitude), (vector_y / magnitude) };
  float unit_vector_vector_x = vector_x / magnitude;
  float unit_vector_vector_y = vector_y / magnitude;
}

void Vector::SetBallVector()
{
  vector_x = 0;
  vector_y = 0;
}
*/
